'use strict';

Object.defineProperty(exports, '__esModule', { value: true });

var index = require('../../../address/lib.esm/index.js');
var index$1 = require('../../../bytes/lib.esm/index.js');
var abstractCoder = require('./abstract-coder.js');

class AddressCoder extends abstractCoder.Coder {
    constructor(localName) {
        super("address", "address", localName, false);
    }
    defaultValue() {
        return "0x0000000000000000000000000000000000000000";
    }
    encode(writer, value) {
        try {
            value = index.getAddress(value);
        }
        catch (error) {
            this._throwError(error.message, value);
        }
        return writer.writeValue(value);
    }
    decode(reader) {
        return index.getAddress(index$1.hexZeroPad(reader.readValue().toHexString(), 20));
    }
}

exports.AddressCoder = AddressCoder;
//# sourceMappingURL=address.js.map
