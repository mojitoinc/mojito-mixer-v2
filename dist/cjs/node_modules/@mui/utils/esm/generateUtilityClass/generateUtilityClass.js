'use strict';

Object.defineProperty(exports, '__esModule', { value: true });

var ClassNameGenerator = require('../ClassNameGenerator/ClassNameGenerator.js');

const globalStateClassesMapping = {
  active: 'active',
  checked: 'checked',
  completed: 'completed',
  disabled: 'disabled',
  error: 'error',
  expanded: 'expanded',
  focused: 'focused',
  focusVisible: 'focusVisible',
  required: 'required',
  selected: 'selected'
};
function generateUtilityClass(componentName, slot, globalStatePrefix = 'Mui') {
  const globalStateClass = globalStateClassesMapping[slot];
  return globalStateClass ? `${globalStatePrefix}-${globalStateClass}` : `${ClassNameGenerator["default"].generate(componentName)}-${slot}`;
}

exports["default"] = generateUtilityClass;
//# sourceMappingURL=generateUtilityClass.js.map
