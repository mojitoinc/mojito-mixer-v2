import '../../utilities/globals/index.js';
import { InvariantError } from '../../../../ts-invariant/lib/invariant.js';

var serializeFetchParameter = function (p, label) {
    var serialized;
    try {
        serialized = JSON.stringify(p);
    }
    catch (e) {
        var parseError = __DEV__ ? new InvariantError("Network request failed. ".concat(label, " is not serializable: ").concat(e.message)) : new InvariantError(23);
        parseError.parseError = e;
        throw parseError;
    }
    return serialized;
};

export { serializeFetchParameter };
//# sourceMappingURL=serializeFetchParameter.js.map
