import countryList from './assets/country.json.js';
import { findEntryByCode } from './utils/index.js';

// Get a country by isoCode.
function getCountryByCode(isoCode) {
    if (!isoCode)
        return undefined;
    return findEntryByCode(countryList, isoCode);
}
// Get a list of all countries.
function getAllCountries() {
    return countryList;
}
var Country = {
    getCountryByCode,
    getAllCountries,
};

export { Country as default };
//# sourceMappingURL=country.js.map
