export type SardineEnvironment = 'production' | 'sandbox';
export declare const SardineConfig: {
    sandbox: {
        host: string;
    };
    production: {
        host: string;
    };
};
